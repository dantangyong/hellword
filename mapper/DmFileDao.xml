<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mht.modules.dm.dao.DmFileDao">
   
	<select id="get" resultType="DmFile">
		SELECT 
			* 
		FROM file 
		WHERE id = #{id}
	</select>
	
	<select id="findList" resultType="DmFile">
		SELECT 
			* 
		FROM file 
	</select>

	<insert id="insert">
		INSERT INTO file(
			id, 
			dict_type_id, 
			file_name,
            file_path,
			file_desc, 
			create_by, 
			create_date, 
			update_by, 
			update_date 
		) VALUES (
			#{id}, 
			#{dictType.id}, 
			#{fileName}, 
			#{filePath}, 
			#{fileDesc}, 
			#{createBy.id}, 
			#{createDate}, 
			#{updateBy.id}, 
			#{updateDate}
			
		)
	</insert>
	
	<update id="update">
		UPDATE file SET 
			dict_type_id = #{dictType.id},
			file_name = #{fileName}, 
			file_path = #{filePath}, 
			file_desc = #{fileDesc}, 
			update_by = #{updateBy.id}, 
			update_date = #{updateDate}, 
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM file 
		WHERE id = #{id}
	</update>
	
</mapper>