<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mht.modules.dm.dao.DataPrepareDao">

	<sql id="dataPrepareColumns">
		dp.id,
		dp.dprepare_name AS "name",
		dp.dprepare_content AS "content",
		dp.dprepare_time AS "time",
		dp.file_id AS "fileId",
		dp.dprepare_desc AS "desc",
		dp.create_by AS "createBy.id",
		dp.create_date,
		dp.update_by AS "updateBy.id",
		dp.update_date
	</sql>
	
	<sql id="dataPrepareJoins">
		LEFT JOIN file f ON f.id = dp.file_id
    </sql>
	<select id="selectAll" resultType="DataPrepare">
		SELECT * FROM data_prepare
	</select>
	
	<select id="get" resultType="DataPrepare">
		SELECT
			<include refid="dataPrepareColumns"/>
		FROM data_prepare dp
		<include refid="dataPrepareJoins"/>
		WHERE dp.id = #{id}
	</select>
	
	<select id="findList" resultType="DataPrepare">
		SELECT
			<include refid="dataPrepareColumns"/>
		FROM define_issue d
		<include refid="dataPrepareJoins"/>
		
		<if test="name != null and name != ''">
				WHERE d.topic like 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
				    <if test="dbName == 'mysql'">CONCAT('%', #{name}, '%')</if>
		</if>
		 
	  </select>
			
	<insert id="insert">
		INSERT INTO data_prepare(
			id, 
			dprepare_name, 
			dprepare_content, 
			dprepare_time, 
			file_id, 
			dprepare_desc, 
			create_by, 
			create_date, 
			update_by, 
			update_date
			
		) VALUES (
			#{id}, 
			#{name}, 
			#{content}, 
			#{time}, 
			#{dmFile.id}, 
			#{desc}, 
			#{createBy.id}, 
			#{createDate}, 
			#{updateBy.id}, 
			#{updateDate}, 
		)
	</insert>
	
	<update id="update">
		UPDATE data_prepare dp
		SET 
			dprepare_name = #{name}, 
			dprepare_content = #{content}, 
			file_id = #{dmFile.id}, 
			dprepare_time = #{time}, 
			d.update_by = #{updateBy.id}, 
			d.update_date = #{updateDate}, 
		WHERE dp.id = #{id}
	</update>
	
	<delete id="delete">
		DELETE FROM data_prepare  
		WHERE id = #{id}
	</delete>
	
	
</mapper>