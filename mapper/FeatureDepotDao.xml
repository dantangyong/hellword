<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mht.modules.dm.dao.FeatureDepotDao">
     
	<sql id="featureDepotColumns">
		fd.id,
		fd.feature_id AS "featureDepot.id",
        fd.dict_id AS "dictValue.id",
		fd.feature_name AS "name",
		fd.feature_desc AS "desc",
		fd.create_by AS "createBy.id",
		fd.create_date,
		fd.update_by AS "updateBy.id",
		fd.update_date
	</sql>
	<select id="get" resultType="FeatureDepot">
		SELECT 
			* 
		FROM feature_depot 
		WHERE id = #{id}
	</select>
	
	<select id="findList" resultType="FeatureDepot">
		SELECT 
			* 
		FROM feature_depot 
	</select>

	<insert id="insert">
		INSERT INTO feature_depot(
			id, 
			feature_id, 
			dict_id,
            feature_name,
            feature_desc,
			create_by, 
			create_date, 
			update_by, 
			update_date 
		) VALUES (
			#{id}, 
			#{featureDepot.id}, 
			#{dictValue.id}, 
			#{name}, 
			#{desc}, 
			#{createBy.id}, 
			#{createDate}, 
			#{updateBy.id}, 
			#{updateDate}
			
		)
	</insert>
	
	<update id="update">
		UPDATE feature_depot SET 
			feature_id = #{featureDepot.id},
			dict_id = #{dictValue.id}, 
			feature_name = #{name}, 
			feature_desc = #{desc}, 
			update_by = #{updateBy.id}, 
			update_date = #{updateDate}, 
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM feature_depot 
		WHERE id = #{id}
	</update>
	
</mapper>