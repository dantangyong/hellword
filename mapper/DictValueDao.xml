<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mht.modules.dm.dao.DictValueDao">
     
	<sql id="dictValueColumns">
		dv.id,
		dv.type_id AS "dictType.id",
		dv.dict_name AS "name",
		dv.orderId AS "orderId",
		dv.parent_id AS "parent.id",
		dv.parent_desc AS "parentDesc",
		dv.create_by AS "createBy.id",
		dv.create_date,
		dv.update_by AS "updateBy.id",
		dv.update_date
	</sql>
	<select id="get" resultType="DictValue">
		SELECT 
			* 
		FROM dict_value 
		WHERE id = #{id}
	</select>
	
	<select id="findList" resultType="DictValue">
		SELECT 
			* 
		FROM dict_value 
		<if test="orderId != null and orderId != ''">
		ORDER BY orderId ASC
		</if>
	</select>

	<insert id="insert">
		INSERT INTO dict_value(
			id, 
			type_id, 
			dict_name, 
			orderId, 
			parent_id,
			parent_desc, 
			create_by, 
			create_date, 
			update_by, 
			update_date 
		) VALUES (
			#{id}, 
			#{dictType.id}, 
			#{name}, 
			#{orderId}, 
			#{parent.id}, 
			#{parentDesc}, 
			#{createBy.id}, 
			#{createDate}, 
			#{updateBy.id}, 
			#{updateDate}
			
		)
	</insert>
	
	<update id="update">
		UPDATE dict_value SET 
			type_id = #{dictType.id}, 
			dict_name = #{name}, 
			orderId = #{orderId}, 
			parent_id = #{parent.id},
			parent_desc = #{parentDesc}, 
			update_by = #{updateBy.id}, 
			update_date = #{updateDate}, 
			
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM dict_value 
		WHERE id = #{id}
	</update>
	
</mapper>