<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mht.modules.dm.dao.DictTypeDao">
    
	<select id="get" resultType="DictType">
		SELECT 
			* 
		FROM dict_type 
		WHERE id = #{id}
	</select>
	
	<select id="findList" resultType="DictType">
		SELECT 
			* 
		FROM dict_type 
		<if test="orderId != null and orderId != ''">
		ORDER BY orderId ASC
		</if>
	</select>

	<insert id="insert">
		INSERT INTO dict_type(
			id, 
			type_code, 
			type_name, 
			orderId, 
			type_desc, 
			create_by, 
			create_date, 
			update_by, 
			update_date 
		) VALUES (
			#{id}, 
			#{code}, 
			#{name}, 
			#{orderId}, 
			#{desc}, 
			#{createBy.id}, 
			#{createDate}, 
			#{updateBy.id}, 
			#{updateDate}
			
		)
	</insert>
	
	<update id="update">
		UPDATE dict_type SET 
			type_code = #{code}, 
			type_name = #{name}, 
			orderId = #{orderId}, 
			type_desc = #{desc}, 
			update_by = #{updateBy.id}, 
			update_date = #{updateDate}, 
			
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM dict_type 
		WHERE id = #{id}
	</update>
	
</mapper>