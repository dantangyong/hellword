<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mht.define.portal.cms.dao.CmsIndexShowTeacherStudentDao">

	<sql id="cmsIndexShowTeacherStudentColumns">
		c.id,
		c.teacherStudent_title AS "title",
		c.teacherStudent_yAxis AS "yAxis",
		c.teacherStudent_legend AS "legend",
		c.teacherStudent_series AS "series",
		c.remarks,
		c.del_flag,
		c.create_by AS "createBy.id",
		c.create_date,
		c.update_by AS "updateBy.id",
		c.update_date
	</sql>
	<select id="get" resultType="CmsIndexShowTeacherStudent">
		SELECT 
			<include refid="cmsIndexShowTeacherStudentColumns"/> 
		FROM mht_portal.cms_index_show_teacherStudent c
		WHERE c.id = #{id}
	</select>
	
	<select id="findList" resultType="CmsIndexShowTeacherStudent">
		SELECT 
			<include refid="cmsIndexShowTeacherStudentColumns"/> 
		FROM mht_portal.cms_index_show_teacherStudent
	</select>
	
	<!--查询师生比统计的legend的data数据-->
	<select id="getTeacherStudentLegendData" resultType="CmsIndexShowTeacherStudent">
		SELECT
			c.teacherStudent_legend AS legend
		FROM
			mht_portal.cms_index_show_teacherStudent c
		GROUP BY
			legend
	</select>
	
	<!--查询师生比统计的yAxis中的data数据-->
	<select id="getTeacherStudentYAxisData" resultType="CmsIndexShowTeacherStudent">
		SELECT
			c.teacherStudent_yAxis AS yAxis
		FROM
			mht_portal.cms_index_show_teacherStudent c
		GROUP BY
			yAxis
	</select>
	
	<!-- 查询师生比统计的series中的data数据 -->
	<select id="getTeacherStudentSeriesData" resultType="CmsIndexShowTeacherStudent">
		SELECT
			c.teacherStudent_series AS series,
			c.teacherStudent_legend AS legend,
			c.teacherStudent_yAxis AS yAxis
		FROM
			mht_portal.cms_index_show_teacherStudent c
		ORDER BY
			legend
	</select>
	
	<insert id="insert">
		INSERT INTO mht_portal.cms_index_show_teacherStudent(
			id, 
			teacherStudent_title
            teacherStudent_yAxis,
            teacherStudent_legend,
			teacherStudent_series, 
			create_by, 
			create_date, 
			update_by, 
			update_date,
			remarks, 
			del_flag 
		) VALUES (
			#{id}, 
			#{title}, 
 			#{yAxis}, 
			#{legend}, 
			#{series}, 
			#{createBy.id}, 
			#{createDate}, 
			#{updateBy.id}, 
			#{updateDate},
			#{remarks}, 
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE mht_portal.cms_index_show_teacherStudent SET 
			analysis_id = #{analysisDepot.id}, 
			teacherStudent_title = #{title}, 
			teacherStudent_yAxis = #{yAxis}, 
			teacherStudent_legend = #{legend}, 
			teacherStudent_series= #{series}
			update_by = #{updateBy.id}, 
			update_date = #{updateDate},
			remarks = #{remarks} 
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM mht_portal.cms_index_show_teacherStudent 
		WHERE id = #{id}
	</update>
	

</mapper>
