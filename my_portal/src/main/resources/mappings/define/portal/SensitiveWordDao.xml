<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mht.define.portal.cms.dao.SensitiveWordDao">
    
	<sql id="sensitiveColumns">
		s.id AS "id",
		s.sensitive_name AS "sensitiveName",
		s.create_date AS "createDate",
		s.update_date AS "updateDate"
	</sql>
	
	<select id="get" resultType="SensitiveWord">
		SELECT 
			<include refid="sensitiveColumns"/>
		FROM mht_portal.solr_sensitive_lib a
		WHERE a.id = #{id}
	</select>
	<select id="findAll" resultType="SensitiveWord" >
		SELECT 
			<include refid="sensitiveColumns"/>
		FROM mht_portal.solr_sensitive_lib s
	</select>
	
	<insert id="insert">
		INSERT INTO mht_portal.solr_sensitive_lib(
			id,
			sensitive_name,
			create_date,
			update_date
		) VALUES (
			#{id},
			#{sensitiveName},
			#{createDate},
			#{updateDate}
		)
	</insert>
	
	<update id="update">
		UPDATE mht_portal.solr_sensitive_lib SET 	
			sensitive_name = #{sensitiveName},
			update_date = #{description}
		WHERE id = #{id}
	</update>
	
</mapper>