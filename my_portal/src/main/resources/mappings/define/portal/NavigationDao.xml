<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mht.define.portal.cms.dao.NavigationDao">
    
	<sql id="cmsNaviColumns">
		a.id AS "id",
		a.naviName AS "naviName",
		a.url AS "url",
		a.parent_id AS "parentId",
		a.status AS "status",
		a.sort AS "sort",
		a.icon AS "icon",
		a.display_setting_id AS "displaySetting.id",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="cmsNaviJoins">
	</sql>
    
	<select id="get" resultType="Navigation">
		SELECT 
			<include refid="cmsNaviColumns"/>
		FROM mht_portal.cms_navigation a
		<include refid="cmsNaviJoins"/>
		WHERE a.id = #{id}
		AND a.del_flag = #{DEL_FLAG_NORMAL}
	</select>
	
	<select id="findChildren" resultType="Navigation">
		SELECT 
			<include refid="cmsNaviColumns"/>
		FROM mht_portal.cms_navigation a
		<include refid="cmsNaviJoins"/>
		<where>
			a.parent_id = #{id}
			AND a.del_flag = 0
		</where>
			ORDER BY a.sort ASC
	</select>
	
	<select id="findByDisplay" resultType="Navigation">
		SELECT 
			<include refid="cmsNaviColumns"/>
		FROM mht_portal.cms_navigation a
			<include refid="cmsNaviJoins"/>
		<where>
			a.del_flag = 0
			AND a.display_setting_id  = #{displaySettingId}
		</where>		
			ORDER BY a.sort ASC
	</select>
	
	<insert id="insert">
		INSERT INTO mht_portal.cms_navigation(
			id,
			naviName,
			url,
			parent_id,
			display_setting_id,
			status,
			sort,
			icon,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{naviName},
			#{url},
			#{parentId},
			#{displaySetting.id},
			#{status},
			#{sort},
			#{icon},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE mht_portal.cms_navigation SET 	
			naviName = #{naviName},
			url = #{url},
			parent_id = #{parentId},
			status = #{status},
			sort = #{sort},
			icon = #{icon},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks},
			del_flag = #{delFlag}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE mht_portal.cms_navigation SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>

</mapper>