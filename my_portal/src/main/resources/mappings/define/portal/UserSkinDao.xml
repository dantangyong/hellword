<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mht.define.portal.cms.dao.UserSkinDao">
    
	<sql id="userSkinColumns">
		a.id,
		a.user_id AS "userId",
		a.is_upload AS "isUpload",
		a.num AS "num",
		a.pid AS "pid",
		a.history AS "history",
		a.image_url AS "imageUrl"
	</sql>
	
	<select id="get" resultType="UserSkin">
		SELECT 
			<include refid="userSkinColumns"/>
		FROM mht_portal.user_skin a
		WHERE a.user_id = #{userId}
	</select>
	
	<insert id="insert">
		INSERT INTO mht_portal.user_skin(
			id,
			user_id,
			is_upload,
			num,
			pid,
			history,
			image_url
		) VALUES (
			#{id},
			#{userId},
			#{isUpload},
			#{num},
			#{pid},
			#{history},
			#{imageUrl}
		)
	</insert>
	
	<update id="update">
		UPDATE mht_portal.user_skin SET 	
			is_upload = #{isUpload},
			num = #{num},
			pid = #{pid},
			history = #{history},
			image_url = #{imageUrl}
		WHERE user_id = #{userId}
	</update>
	
</mapper>